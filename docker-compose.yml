version: '3.8'

services:
  scout-platform:
    build: .
    volumes:
      - .:/app
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - GOOGLE_API_KEY=${GEMINI_API_KEY}